using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RoomController : MonoBehaviour
{
    /*
    private class SO_SubscribedVariable<TypeOfState> : ScriptableObject
    {
        private Action<TypeOfState> variableToSubscribeTo; //= S_Global.Current.PrologueLvl.action;

        [SerializeField]
        private TypeOfState DesiredState;


        public void AttachToActionEvent(Action<TypeOfState> OnDesiredState)
        {
            variableToSubscribeTo -= OnDesiredState;
            variableToSubscribeTo += OnDesiredState;
        }
    }



    [Serializable]
    private class RoomControllerWrapper<T>
    {
        [SerializeField]
        private SO_SubscribedVariable<T> ourVariableWeChoose;

        [SerializeField]
        private List<GameObject> objectsToEnable;

        [SerializeField]
        private List<GameObject> objectsToDisable;

        [SerializeField]
        private List<SO_Reaction> reactions;

    }


    [SerializeField]
    List<RoomControllerWrapper> rcws;

    private void Awake()
    {
        foreach(RoomControllerWrapper rcw in )
    }

    */
}
